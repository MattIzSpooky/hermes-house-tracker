include:
  - hermes-backend/docker-compose.yml

services:
  frontend:
    build: ./hermes-frontend
    ports:
      - "4200:80"
    environment:
      BACKEND_URL: http://backend:8080
    depends_on:
      - backend
